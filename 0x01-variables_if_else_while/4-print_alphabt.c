#include <stdio.h>

/**
* main - Entry Point
*
* Return: returns nothing
*/
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if (c == 'e' || C == 'q')
		{
			c++;
		} else
		{
			putchar(c);
			c++;
		}
	}
	putchar('\n');
	return (0);
}
