#include "main.h"
/**
* _abs - computes the absolute value of an integer.
*
* Return: returns the absolute value of an integer.
*/
int print_sign(int)
{
	if (int >= 0)
	{
		return (int);
	}		
	else
	{
		return (-int);
	}
}
